# build outputs
build/
*.apk
*.idsig
tools/out/

# third-party binaries: not vendored, run fetch_deps.ps1
app/assets/models/
app/jniLibs/
libs/

# signing material: never publish
*.keystore
*.jks

# tokenizer cross-check artifacts
tools/probe_java.txt
tools/probe_py.txt

# keep the bundled demo page (only the model dir is fetched)
!app/assets/demo-chat.html

# 调试时的过程截图，不要入库
docs/_*.png
